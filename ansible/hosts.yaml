---
control:
  hosts:
    local-control:
      ansible_connection: local
    control-vm:
      ansible_host: control

appservers:
    hosts:
      appserver-vm:
        ansible_host: appserver

dbservers:
    hosts:
      dbserver-vm:
          ansible_host: dbserver

azure-hosts:
  hosts:
    devops-vm-1:
      ansible_host: "{{ vm_ip }}"