[Unit]
Description=spring boot email service daemon
After=network.target

[Service]
User={{appuser}}
Group={{appgroup}}
WorkingDirectory={{service.workingdir}}
ExecStart={{service.execstart}}

[Install]
WantedBy=multi-user.target