{
  "name": "akinita-api",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd Akinita/Akinita && chmod +x ./mvnw && ./mvnw clean package"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd Akinita/Akinita && chmod +x ./mvnw && ./mvnw test"
      }
    }
  }
}
